<!DOCTYPE html>
<html lang="en">
    <head>
        <!-- The head tag contains all the meta elements containing metadata about the page, along with links to other assets such as the style
        sheet css, also includes the main title of the page. -->
        <meta charset="utf-8">
        <meta name="description" content="Lists available chinese dishes offered at the restaurant.">        
        <meta name="keywords" content="Town, menu, Chinese cuisine, food, quality, variety, breakfast, lunch, dinner">
        <meta name="author" content="Kevin Caballero">       
        <meta name="viewport" content="width = device-width, initial-scale=1">        
        <link rel="stylesheet" href="css/stylesheet.css"> 
        <title>Menu</title>
        <style>
            /* Main image for menu */
            .menuimage {
                display: block;
                width: 70%;
                height: auto;
                margin: 24px auto;
                border: 4px solid #be1e2d50;
                border-radius: 10px;
            }
            /* Menu heading format */
            .menuhead {
                display: block; 
                background-color: rgba(0,0,0, 0.5);
                color: #d4a18d;
                font-family: 'majesti_bannerheavy';
                font-size: 32px;
                border: 3px solid #be1e2d50;
                border-radius: 10px;
                width: 70%;
                padding: 2px;
                text-align: center;
                margin: auto;
                margin-top: 24px;
                margin-bottom: 24px;
            }
            /* Menu options formatting */
            .foodtype {
                text-align: center;
                text-decoration-line: underline;
                color: #d4a18d;
                font-size: 36px;
                padding: 30px;
            }
            @media screen and (max-width: 650px) {
                .column {
                    width: 100%;
                    display: block;
                }   
            }
            .menurow{
                background-color: #41393d;
            }
            .menucolumn {
                float: left;
                align-items: center;
                width: 25%;
                margin-bottom: 24px;
                padding: 0 6.5rem;
            }
            .menucard {
                box-shadow: 0 8px 16px 0 rgba(0,0,0, 0.4);
                background-color: #d4a18d;
            }
            .menucontainer {
                padding: 0 32px;
                font-size: 24px;
                font-family: 'majesti_bannermedium';
                text-align: center;
                color: #41393d;
            }
            .menucontainer::after, .menurow::after {
                content: "";
                clear: both;
                display: table;
            }
            .title {
                font-size: 32px;
                font-family: 'majesti_bannerbold';
            }
            .food {
                width: 100%;
                height: auto;
            }
            /* Copy right statement formatting */
            .copyright {
                text-align: center;
                font-size: 12px;
                color: white;
            }
        </style>        
    </head>
    <body>
        {{> header}}
        <div class="menuimgcontainer">
            <div>
                <Img class="menuimage" src="images/Chinese-dishes.jpg" alt="Image containing several Chinese dishes"> 
            </div>                
        </div>
        <div class="menuhead">
            <!-- Menu heading -->
            <h1> Menu </h1>
        </div>
        <!-- Food heading - Breakfast -->
        <div class="foodtype">
            <h3>Breakfast</h3>
        </div> 
        <!-- Content cards containing food choices for customers -->
        <div class="menurow">
            {{#each meals.breakfast}}
            <div class="menucolumn">
                <div class="menucard">
                    <a href="/api/meals/{{this.code}}"><img class="food" src="images/{{this.image}}" alt="Chinese porridge"></a>
                    <div class="menucontainer">
                        <h2 class="title">{{this.name}}</h2>
                        {{{this.description}}}
                        <p>{{this.price}}</p>
                    </div>
                </div>
            </div>
            {{/each}}
        </div>
        <!-- Food heading - Lunch -->
        <div class="foodtype">
            <h3>Lunch</h3>
        </div>
        <div class="menurow">
             {{#each meals.lunch}}
            <div class="menucolumn">
                <div class="menucard">
                    <a href="/api/meals/{{this.code}}"><img class="food" src="images/{{this.image}}" alt="Chinese porridge"></a>
                    <div class="menucontainer">
                        <h2 class="title">{{this.name}}</h2>
                        {{{this.description}}}
                        <p>{{this.price}}</p>
                    </div>
                </div>
            </div>
            {{/each}}
        </div>
        <!-- Food heading - Dinner -->        
        <div class="foodtype">
            <h3>Dinner</h3>
        </div>
        <div class="menurow">
             {{#each meals.dinner}}
            <div class="menucolumn">
                <div class="menucard">
                    <a href="/api/meals/{{this.code}}"><img class="food" src="images/{{this.image}}" alt="Chinese porridge"></a>
                    <div class="menucontainer">
                        <h2 class="title">{{this.name}}</h2>
                        {{{this.description}}}
                        <p>{{this.price}}</p>
                    </div>
                </div>
            </div>
            {{/each}}
        </div>        
        <!-- Contains the footer info. for each page listing the location, hours, email, as well as phone number of the restaurant. -->
        {{> footer}}     
    </body>
</html>